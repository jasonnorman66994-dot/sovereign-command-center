#!/usr/bin/env sh
set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"

echo "Running full smoke check before push..."
powershell -NoProfile -ExecutionPolicy Bypass -File "$REPO_ROOT/smoke_check.ps1" -Mode Full
